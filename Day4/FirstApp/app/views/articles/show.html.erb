<h1><%= @article.title %></h1>

<p><%= @article.body %></p>
<p>Status: <%= @article.status %></p>
<p>Image: <%= image_tag @article.image, size: "300x300" if @article.image.attached? %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
  <li><%= link_to "Report",report_article_path(@article), data:{turbo_method: :get} %></li>
</ul>
<p>Report Count: <%= @article.report_count || 0 %></p>
<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment:</h2>
<%= render "comments/form" %>